import React from 'react';

class LoginPage extends React.Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}

LoginPage.propTypes = {
};

export default LoginPage;
